# 📱 ملخص مشروع منظومة تطبيق البصمة الذكي (Basma Attendance System)

**اسم المنظومة**: تطبيق البصمة الذكي - Basma Attendance  
**إصدار النظام الحالي**: `v1.14.0` (Semantic Versioning)  
**البيئة التشغيلية**: Production (الإنتاج الفعلي) - [رابط المنظومة](https://basma-attendance-gold.vercel.app)  
**التوقيت الرسمي للعمليات**: أفريقيا / طرابلس (`Africa/Tripoli` UTC+2)  

---

## 📌 1. نبذة عن المشروع والهدف الأساسي
منظومة **"بصمة"** هي تطبيق ويب متقدم ومتوافق مع أجهزة الجوال (PWA) مخصص لإدارة وتسجيل حضور وانصراف الموظفين بدقة عالية، وتأمين العمليات الميدانية، وتوفير محرك تقارير إدارية وتحليلات موثوقة وقابلة للتدقيق.

تهدف المنظومة إلى:
1. **استبدال أجهزة البصمة التقليدية**: ببديل ذكي وفوري يعمل عبر جوالات الموظفين (iOS Safari PWA & Android Chrome PWA).
2. **منع التلاعب والتزوير**: عبر محرك ثقة الموقع (Location Confidence Engine) والأجهزة المعتمدة (Approved Device Model) وتحديات الـ OTP المشفرة.
3. **تطبيق سياسة الحضور والغياب المعتمدة**: مع مراعاة فترات السماح، الورديات المرنة، الورديات الليلية، تناوب يوم الجمعة، والاستراحات المدفوعة.
4. **توفير تقارير إدارية موحدة (P0 Core Reports)**: تعتمد على محرك حسابات مركزي بالسيرفر وتراعي صلاحيات الوصول (RBAC).

---

## 🛠️ 2. البنية التحتية والتقنيات المستعملة (Tech Stack)

* **الواجهة الأمامية والمنطق (Frontend & Logic)**:
  * Next.js 14.1.0 (App Router, Server Actions, API Routes)
  * React 18, TypeScript, Tailwind CSS
  * واجهة موظف مبتكرة بنمط التبسيط العصري الفاخر (`Modern Premium Minimal`) بدعم كامل للـ RTL واللغة العربية.
* **قاعدة البيانات والطبقة الخلفية (Backend & Database)**:
  * Supabase Cloud PostgreSQL عبر Prisma ORM v5.22.0 مع إدارة التغييرات عبر Prisma Migrations الرسمية.
  * نظام جلسات مشفر (JWT) مع تشفير كلمات المرور بـ bcrypt وصمام أمان لحظر التخمين (Rate Limiting).
* **الإشعارات والتنبيهات (Realtime & Push)**:
  * بث الإشعارات المباشر (Server-Sent Events - SSE) مع نظام Smart Fallback Polling كل 15 ثانية.
  * إشعارات الجوال الفورية (VAPID-authenticated Web Push) التي تصل للموظفين حتى عند إغلاق التطبيق.

---

## 🏢 3. القرارات الإدارية وحسابات الحضور المعتمدة (v1.14.0)

تمت برمجة واعتماد 7 قرارات إدارية رسمية داخل محرك التقارير المركزية (`src/lib/reporting/`):

1. **فترة السماح وتأخير الدخول (Grace Period & Late)**:
   * لا يحسب أي تأخير إذا سجل الموظف دخوله داخل فترة السماح.
   * عند تجاوز فترة السماح، يبدأ حساب التأخير فقط من بعد نهاية فترة السماح:
     $$\text{lateMinutes} = \max(0, \text{actualCheckIn} - (\text{scheduledStart} + \text{gracePeriod}))$$
2. **ساعات العمل الإضافية (Overtime Policy)**:
   * تعرض ساعات العمل الزائدة كـ `Potential Overtime` بشكل استرشادي فقط دون إجراء خصومات أو مستحقات مالية تلقائية قبل تفعيل دورة الاعتماد.
3. **سياسة الاستراحة (Break Policy)**:
   * الاستراحة المسموحة تعتبر **مدفوعة الأجر** ولا تخصم من ساعات العمل الأساسية.
   * يتم تتبع التجاوز فوق الحد المسموح بشكل مستقل كـ `BREAK_EXCESS_MINUTES` دون خصم مالي من الراتب.
4. **الورديات المرنة (Flexible Shifts)**:
   * لا نعتمد وقت الدخول لحساب التأخير التقليدي، بل يتم تقييم عجز ساعات العمل المطلوبة `WORK_HOURS_DEFICIT`.
5. **الانصراف المفقود (Missing Check-Out)**:
   * إذا سجل الموظف الدخول ولم يسجل الانصراف، تصنف الحالة كـ `INCOMPLETE_ATTENDANCE` وتظهر ساعات العمل كـ `null` دون اصطناع وقت خروج افتراضي.
6. **التعديلات الإدارية (Admin Adjustment)**:
   * أي سجل معدل يوسم بـ `ADMIN_ADJUSTED` مع الاحتفاظ بسجل التدقيق والتفاصيل المتاحة.
7. **تناوب يوم الجمعة (Friday Rotation)**:
   * يعتبر يوم الجمعة `NOT_SCHEDULED` (غير مجدول) إلا إذا كان الموظف مجدولاً رسمياً للعمل في ذلك اليوم، وعندها فقط يحسب كـ `ABSENT` في حال عدم الحضور.

---

## 📊 4. حزمة التقارير الأساسية المفعلة (P0 Core Reports)

يقدم محرك التقارير المركزي 6 تقارير إدارية رئيسية تعمل بالسيرفر ومحمية بنظام RBAC:

* **P0-01: التقرير المباشر لحضور اليوم (`/admin/reports/today`)**:
  * متابعة لحظية لعدّادات الموظفين المتوقع حضورهم، المتواجدين الآن، المتأخرين، في استراحة، المنصرفين، والحالات التي تحتاج مراجعة.
* **P0-02: تقرير الحضور اليومي التفصيلي (`GET /api/reports/daily`)**:
  * جدول تفصيلي لكل موظف يشمل مواعيد الوردية، وقت الدخول/الخروج، التأخير، ساعات العمل الفعلية، وساعات الاستراحة والتنبيهات.
* **P0-03: الملخص الشهري للحضور (`GET /api/reports/monthly`)**:
  * إحصائيات شاملة لكل موظف على مدار الشهر (أيام الحضور، الغياب، الإجازات، مجموع دقائق التأخير، وساعات العمل الكلية).
* **P0-04: تقرير التأخير والغياب (`GET /api/reports/late-absence`)**:
  * تقرير متخصص لرصد حالات التأخير، الخروج المبكر، الغياب، وعجز ساعات العمل المرنة.
* **P0-05: تقرير التجاوزات والحالات الخاصة (`GET /api/reports/exceptions`)**:
  * فلترة فورية للحالات التي تتطلب تدخل الإدارة (الانصراف المفقود، تجاوز الاستراحة، طلبات تأكيد الموقع، التعديلات اليدوية، ومحاولات البصمة خارج النطاق).
* **P0-06: الملف الشامل لحضور الموظف (`GET /api/reports/employees/[id]`)**:
  * بطاقة تفصيلية لكل موظف تعرض بطاقات الملخص، السجل اليومي، وتاريخ التجاوزات بدون إظهار أي بيانات سرية للجهاز.

---

## 🔐 5. نظام الأمان والتحكم بالصلاحيات (RBAC & Security)

تم بناء تصفية البيانات في طبقة السيرفر (Server-side RBAC) لضمان حماية البيانات:

| الدور (Role) | نطاق الوصول المصرح به (Data Scope) |
| :--- | :--- |
| **`SUPER_ADMIN`** / **`ADMIN`** / **`HR`** | كافة بيانات المنظومة وجميع الفروع والأقسام. |
| **`BRANCH_MANAGER`** | بيانات الفرع الرئيسي للفروع المصرح بها فقط (`primaryBranchId` أو `EmployeeBranch`). |
| **`EMPLOYEE`** | البيانات الشخصية الخاصة بالموظف فقط (`own employeeId`). |

---

## 📈 6. مراحل المشروع والتاريخ التشغيلي

```
Phase 01 - 07: التأسيس، إدارة الأجهزة، وتثبيت قواعد البيانات (v1.0.0 - v1.0.7)
Phase 08:      محرك النسخ الاحتياطي والتعافي من الكوارث BACKUP & RESTORE (v1.0.8)
Phase 09:      معالجة قيود بيئة iOS Sandbox واختبار أجهزة iPhone الحقيقية (v1.0.9)
Phase 10:      التشغيل التجريبي المحكوم Controlled Pilot (v1.10.0)
Phase 10.5-10.7: إعادة تصميم واجهة الموظف Modern Premium Minimal وتدقيقها ميدانياً (v1.11.0)
Phase 10.8:    إطلاق إشعارات الجوال الفورية Web Push Notifications (v1.12.0)
Phase 11:      التعميم الشامل النهائي على الإنتاج Full Production Rollout (v1.13.0)
Phase 12A:     تصميم ومراجعة منظومة التقارير والتحليلات الإدارية (v1.13.0)
Phase 12B.1:   تطوير وتشغيل محرك التقارير المركزي P0 Core Reporting Engine (v1.14.0) 🟢 [الحالية]
Phase 12B.2:   التحليلات التنفيذية والتصدير Excel/PDF (المرحلة القادمة)
```

---

## 📄 7. ملفات التوثيق الرئيسية في المشروع

* **`PROJECT_SUMMARY.md`**: هذا الملف (ملخص شامل وموثق للمشروع).
* **`docs/PROJECT_STATUS.md`**: حالة المنظومة التشغيلية الحية والنسخة الحالية.
* **`docs/DECISIONS.md`**: سجل القرارات الفنية والمعمارية السبعة عشر (Decisions 1-20).
* **`docs/REPORTING_METRICS_DICTIONARY.md`**: قاموس المؤشرات الحسابية ومعادلات التقارير.
* **`docs/reports/PHASE_12B_1_CORE_REPORTING_ENGINE_REPORT.md`**: التقرير النهائي لاختبارات وتدشين محرك التقارير.
* **`docs/CHANGELOG.md`**: سجل التغييرات اليومي والتحديثات البرمجية.
